{
  "global": {
    "Space": "open command menu",
    "Ctrl+C": "quit",
    "Ctrl+S": "quick save",
    "u": "undo (works everywhere except modals and text entry; while recording, scraps the newest layer — the in-flight pass first, then committed takes one per press — and the transport keeps rolling)",
    "Ctrl+R": "redo (a peeled take included; recording new material clears redo)",
    "Tab": "cycle panes / tabs",
    "Shift+Tab": "prev pane",
    "Esc": "back / close / clear / unlock",
    "+/-": "adjust BPM",
    "recording_automation": "mod wheel, pitch bend and channel aftertouch are recorded with the notes, survive every edit and the session file, and play back; X in the piano roll erases them from a clip",
    "sustain_pedal": "CC64 held during recording becomes note length at commit: releases under the pedal slide to the pedal-up"
  },
  "space_menu": {
    "1": "focus transport",
    "2": "focus tracks",
    "3": "focus clip view",
    "p": "play/pause",
    "r": "toggle recording",
    "l": "edit loop region",
    "m": "toggle metronome",
    "!": "panic (kill all sound)",
    "a": "add instrument track",
    "s": "save project",
    "o": "open project",
    "d": "delete selected track/clip",
    "e": "enter edit mode (piano roll)",
    "q": "quantize notes to grid",
    "w": "write/recall instrument presets",
    "v": "cycle color theme",
    "h": "help topics"
  },
  "preset_browser": {
    "description": "Space+W on an instrument track. Lists that instrument's saved presets, with a row at the top to save the current panel. Banks live in ~/.phosphor/presets/<instrument>.json, one file per instrument.",
    "j/k": "navigate rows",
    "Enter_on_save_row": "name and save the current panel",
    "Enter_on_preset": "load the preset into the track",
    "d": "delete the selected preset (y/n confirmation)",
    "Esc": "close the browser",
    "overwrite": "saving under a name the bank already holds rewrites that slot, after a y/n confirmation — names are slots, so no two presets can share one",
    "limits": "128 presets per instrument, 32 characters per name",
    "refusals": "a preset is refused rather than loaded when it was saved for a different instrument, with a different number of controls, or against an older panel layout"
  },
  "tracks_pane": {
    "not_selected": {
      "j/k": "navigate between tracks",
      "Enter": "select track",
      "q": "quit"
    },
    "selected": {
      "h/l": "navigate elements (label→fx→vol→mute→solo→arm→clips)",
      "Enter_on_clip": "lock clip (enter clip-locked mode)",
      "m": "toggle mute",
      "s": "toggle solo",
      "r": "toggle record arm",
      "R": "toggle loop record",
      "y": "yank (copy) clip (on a clip element); on the label: yank the track's whole arrangement",
      "p": "paste clip after current (when on clip element; refuses to overlap)",
      "P": "paste yanked clip(s) onto this track at the same bars (layering; refuses to overlap)",
      "d": "duplicate clip (when on clip element)",
      "1-9": "jump to clip by number",
      "Esc": "deselect track",
      "Enter_on_volume": "lock fader (enter volume-locked mode)",
      "D": "duplicate the whole track — instrument, panel, effects, mix, clips — directly below (one undo step)",
      "n": "on the label element: rename the track (trimmed, 8 chars max, undoable)"
    },
    "clip_locked": {
      "description": "Enter on a clip enters this mode. h/l moves clip, Shift stretches, Ctrl trims.",
      "h/l": "move clip left/right by one beat",
      "Shift+H/L": "shrink/extend right edge by one beat",
      "Ctrl+H/L": "trim/extend left edge by one beat",
      "y": "yank clip",
      "p": "paste after (refuses to overlap)",
      "P": "paste to track",
      "d": "duplicate clip",
      "Esc": "unlock clip (back to element navigation)"
    },
    "volume_locked": {
      "description": "Enter on the volume element enters this mode. The header cell shows the fader in dB relative to unity.",
      "h/l": "fader down/up by 1 dB (bottom = silent, unity = 0 dB, top = +6 dB)",
      "Esc": "release fader (back to element navigation)",
      "Enter": "release fader (back to element navigation)"
    },
    "arm_display": "an armed track's dot draws dim until the track is selected — MIDI records onto the selected track only"
  },
  "clip_view": {
    "focus_switch": {
      "Tab": "cycle tabs: trk fx → synth → inst config → piano → settings → trk fx",
      "h_from_piano": "switch focus to FX panel",
      "l_from_fx": "switch focus to piano roll"
    },
    "fx_panel_synth_tab": {
      "j/k": "navigate synth parameters",
      "h/l": "decrease/increase parameter value"
    },
    "settings_tab": {
      "j/k": "navigate settings (grid, snap, velocity)",
      "h/l": "adjust setting value",
      "rec_quant": "record quantize: off / 1/32 / 1/16 / 1/8 — takes snap to this grid as they commit; hits pulled onto one line collapse to the harder"
    }
  },
  "piano_roll_navigation": {
    "description": "Default mode when viewing the piano roll (not in edit mode).",
    "n": "toggle note at cursor",
    "h/l": "navigate columns (or MOVE highlighted notes left/right if highlights exist)",
    "j/k": "scroll up/down (or MOVE highlighted notes up/down if highlights exist)",
    "1-9": "jump to column by number",
    "Enter": "select column (enter Right-Left-Trick mode)",
    "Shift+H/L": "start/expand column highlight",
    "Shift+J/K": "start/expand row highlight",
    "d": "delete notes in highlighted region",
    "y": "yank notes in highlighted region",
    "p": "paste yanked notes at cursor/highlight",
    "Enter_with_highlights": "lock highlights for STRETCHING (Right-Left-Trick on group)",
    "Esc": "clear highlights, or exit piano roll if no highlights",
    "X": "clear the clip's recorded controller data (mod wheel, pitch bend, aftertouch; undoable)",
    "{ / }": "jump the cursor down / up a whole octave (12 semitones) at once",
    "[ / ]": "snap the cursor to the nearest pitch below / above that has a note",
    "_framing": "opening a clip frames the view on its notes, so you start on the music",
    "g": "jump the cursor to the playhead's column, landing on a sounding note there when one exists"
  },
  "piano_roll_highlight_locked": {
    "description": "After highlighting notes and pressing Enter, h/l/H/L stretch the group.",
    "h/l": "adjust LEFT edge of all highlighted notes",
    "H/L": "adjust RIGHT edge of all highlighted notes",
    "d": "delete highlighted notes",
    "Esc": "unlock highlights, back to navigation"
  },
  "piano_roll_column_selected": {
    "description": "Right-Left-Trick: column is locked. h/l adjusts note edges.",
    "h/l": "adjust LEFT edge of all notes in column",
    "H/L": "adjust RIGHT edge of all notes in column",
    "j/k": "enter row mode (select individual note)",
    "Esc": "back to navigation mode"
  },
  "piano_roll_row_mode": {
    "description": "Single note selected via column→row drill-down.",
    "h/l": "adjust LEFT edge of single note",
    "H/L": "adjust RIGHT edge of single note",
    "j/k": "move between notes in column",
    "n": "draw/toggle note at cursor",
    "Esc": "back to column mode"
  },
  "edit_mode": {
    "description": "Space+E enters edit mode. Navigate between individual notes, select, move, delete.",
    "enter": "Space+E (only from piano roll view)",
    "navigate": {
      "j/k": "move to next note UP/DOWN within same column",
      "h/l": "jump to nearest note in prev/next column",
      "Enter": "select cursor note for moving (single-note select)",
      "d": "delete cursor note (undoable)",
      "u": "undo",
      "Esc": "exit edit mode",
      ", / .": "velocity of the cursor note down/up by 8 (< / > by 24); the header shows the value"
    },
    "selecting": {
      "trigger": "Shift+direction from navigate mode",
      "Shift+j/k": "extend selection up/down in column",
      "Shift+h/l": "extend selection to prev/next column",
      "d": "delete all selected notes (undoable)",
      "plain_direction": "transition to moving mode",
      "Esc": "clear selection, back to navigate"
    },
    "moving": {
      "trigger": "plain direction after selecting, or Enter on a single note",
      "h/l": "move selected notes left/right by grid step",
      "j/k": "move selected notes up/down by semitone",
      "Shift+H/L": "stretch RIGHT edge of selected notes (duration)",
      "Shift+J/K": "stretch LEFT edge of selected notes (start position)",
      "d": "delete all selected notes",
      "Esc": "lock notes in place, clear selection",
      ", / .": "velocity of the whole selection down/up (< / > in strides)"
    },
    "velocity_display": "notes render brighter the harder they were hit, so dynamics are visible at a glance",
    "m": "toggle mute on the selection or cursor note — a muted note stays in the clip (drawn dark, struck through) but produces no sound; one undo step"
  },
  "quantize_modal": {
    "description": "Space+Q opens quantize modal when a clip is selected.",
    "j/k": "navigate rows (grid, strength, apply)",
    "h/l": "adjust selected value (grid resolution or strength %)",
    "Enter": "apply quantize (when on apply button)",
    "Esc": "close modal"
  },
  "loop_editor": {
    "enter_via": "Space+L",
    "h/l": "move loop start left/right by one grid step",
    "H/L": "move loop end left/right by one grid step",
    "g": "cycle the marker grid: bar / beat / 1/8 / 1/16",
    "j/k": "slide the whole brace forward/back by one grid step",
    "J/K": "leap the brace by its own length",
    "y": "lift the section (all tracks)",
    "x_or_d": "cut the section (one undo across all tracks)",
    "p": "stamp at the brace (replaces under the stamp), then leapfrog",
    "P": "layer at the brace (merges)",
    "Enter": "toggle loop enabled/disabled",
    "Esc": "exit loop editor"
  },
  "transport_pane": {
    "h/l": "navigate transport elements (BPM, Record, Loop, Metronome)",
    "Enter": "activate element (edit BPM, toggle record, etc.)",
    "h/l_while_editing_bpm": "decrease/increase BPM",
    "Esc": "release element",
    "count_in": "Enter on the count element cycles off → 1 bar → 2 bars. With count-in set AND record armed (Space+r) AND play pressed — all three — the bars click down before recording rolls. R counts in too; any transport key during the countdown cancels it.",
    "take_mode": "Enter on the take element toggles overdub (passes layer up) vs re-record (R clears the loop range first, undoable); while recording the cell shows the running pass count",
    "punch_in": "R while the transport is already playing punches recording in at the playhead — no rewind, no count-in"
  },
  "grid_settings": {
    "resolutions": [
      "1/4",
      "1/8",
      "1/16",
      "1/32",
      "1/4T",
      "1/8T",
      "1/16T"
    ],
    "snap": "on/off — notes snap to grid when moved in edit mode",
    "default_velocity": "1-127 — velocity for newly drawn notes"
  },
  "clip_behavior": {
    "shrink_right_edge": "Notes past the new boundary are hidden (not deleted). Expanding restores them.",
    "trim_left_edge": "Notes before the new start are hidden. Expanding restores them.",
    "move_clip": "h/l in clip-locked mode. Collision detection prevents overlap.",
    "duplicate": "d in clip-locked mode. Creates copy right after the clip.",
    "delete": "Space+D with y/n confirmation. Selects adjacent clip after delete.",
    "piano_roll_sync": "Clip view always shows the selected clip. Stretching/trimming updates piano roll in real time."
  },
  "automation_lane": {
    "description": "A in the piano roll opens a controller lane under the note grid, sharing the column grid so points sit under the notes in each column.",
    "A": "open the lane and give it the keys; A again hands them back to the note grid",
    "k/j": "raise/lower the controller value at the cursor column and draw a point (K/J = bigger steps)",
    "h/l": "walk columns; the value carries along, so a ramp is walk-right-and-raise",
    "[ / ]": "cycle which controller the lane shows (recorded streams first, then mod/bend/aftertouch)",
    "d": "clear the point in the cursor column",
    "Esc": "close the lane",
    "note": "an empty clip offers mod, bend and aftertouch to draw from scratch; a recorded sweep opens on its own stream; edits are undoable and play back",
    "r": "ramp: draw both end values with k/j, then r fills every column back to the previous point with a straight line (one undo step)"
  },
  "midi_fx": {
    "_about": "MIDI effects run on the notes before the instrument, on live playing AND clip playback; recording stores the raw keys you played, never the generated notes — change the effect later and old takes play through the new setting",
    "add": "a in the [trk fx] tab (or enter on the fx cell) opens the add menu — 'arp · midi' is the MIDI entry; it lands above the audio effects in the same list",
    "arp_panel": "enter on the arp opens its panel: j/k picks a knob, h/l adjusts (H/L strides), b bypass, esc back",
    "arp_params": "style up/down/updown/played/chord/random · rate 1/4..1/32 incl. dotted+triplet · gate 1-200% · octaves 1-4 · latch off/hold (hold keeps the chord after hands leave; a new chord replaces it) · vel 0=as played or fixed · swing 50-75% (54/56/58 are the MPC feels)",
    "bypass_remove": "m (or b) mutes/bypasses the effect under the cursor — in the chain list and inside every panel (a muted MIDI effect flushes its held notes so nothing hangs); d removes with confirm, u undoes either",
    "order": "MIDI effects always lead the chain — they run on notes, the audio effects run on sound",
    "chord_device": "chord · midi: below the split every key plays its scale degree's full chord — root+scale pick the key, color picks triad/7th/9th/lush, voicing picks close/drop2/rootless A·B/quartal/spread, bass adds the root below, strum rolls it; a register lock voices each chord near the last so progressions walk",
    "chain": "chord before arp always (auto-ordered): one key → full chord → arpeggiated run",
    "quality_movement": "the minor→major→sus→dim magic: park the root, ride the color and scale knobs — every chord stays voiced near the last, so quality changes read as light changing, not jumps",
    "ghost_notes": "when a midi device is active, the piano roll draws its output as faint shaded notes behind your played notes — what you see is what will sound; they update as you turn knobs and vanish on bypass",
    "commit": "c in the [trk fx] tab prints the rack's output into the viewed clip as real editable notes and bypasses the devices (so it can't transform twice); one undo restores your played notes AND the live rack together",
    "arp_presets": "1-4 in the arp panel: rhodes 8ths (up 1/8 gate75 swing56), dilla 16ths (played 1/16 gate45 swing58), wide updown (2 oct), chord pulse (1/8 gate40 swing54) — each one undo step",
    "progression_mode": "chord device mode knob: 'scale' (default, key = scale degree) or 'prog' — white keys walk a stored progression (C = first chord, D = second …, wrapping; black keys borrow the white below). prog knob picks from 8: 2-5-1 (Dm9 G13 Cmaj9), gospel V (G9sus4 G7b9 Cmaj9 C6/9), backdoor (Fm7 Bb9 Cmaj7), church IV→iv (Fmaj9 Fm6/9 Cmaj7/E), turnaround (A7b9 Dm9 G13sus4 Cmaj9), half-step (Dm9 Dbmaj7), neo loop (Bbm11 Gm9 Gbmaj13 Ebm11), quality cycle (Cm9 Cmaj9 C9sus4 C7b9 — one root, moving light). The root knob transposes every progression; the register lock keeps the voicings walking.",
    "progression_editor": "e in the chord panel opens the progression editor: j/k picks a chord row, tab picks the column (root/quality/bass), h/l turns it; a adds a chord (copies the cursor's), d removes, [ ] browses your saved library, n names it, s saves to the library (~/.phosphor/progressions.json), enter loads it into the device (flips mode to prog, prog to 'user'). 16 qualities to pick from — maj7/maj9/6-9/maj13, m7/m9/m11/m6-9, 7/9/13/9sus4, 7b9/7#9/mMaj7/dim7 — plus a slash bass per chord. Sessions store the resolved chords, so editing the library later never changes a saved song",
    "humanize": "the 'human' knob on both devices (0-100%): generated notes drag up to 10ms late and vary up to ±12 in velocity, like hands instead of a machine — steps never fire early, ghosts/commit stay deterministic",
    "learn": "r in the progression editor arms learn: play any chord on the controller and lift — the row becomes exactly that voicing, spread and all (root = lowest key). Turning its quality knob afterwards returns it to the dictionary. Learned chords save to the library and into sessions like any other",
    "live_knobs": "chord-shaping knobs (root, scale, color, voicing, bass, mode, prog — and loading a progression) re-voice chords that are ALREADY SOUNDING: removed tones release, new tones strike, common tones keep ringing. No need to stop/restart or re-press keys. Split, strum and human shape future presses only",
    "split_warning": "if a chord device is active and every note in the viewed clip is at/above the split, the status bar says so once — those notes pass through unchanged, which is why knobs would sound dead on them"
  },
  "sequencer_yank_paste": {
    "step": "y with the cursor on the grid OR inside the step panel yanks the one step under it — pitch, chord, voicing, gate, accent; p drops it on the cursor position (one undo step). y on an empty step refuses",
    "pattern": "y anywhere off the grid (the pattern/instrument row, the slots row) yanks the whole pattern as a block; p pastes it into the selected slot of whichever track's sequencer you're on — it crosses tracks and carries the notes, the length, the rate, everything (one undo step)"
  },
  "practice_room": {
    "_about": "spc+f on an instrument track opens 'fingers' — a jazz-piano technique trainer. Your MIDI controller keeps sounding the selected track's synth; the room listens with microsecond timestamps and judges. Everything is generated: no canned content, any key.",
    "drills": "major scales, harmonic minor scales, chromatic (all with verified published fingerings, drawn on the keys), Hanon no.1, major arpeggios, shell ii-V-I around the cycle of fourths, rootless A/B ii-V-I, Charleston comping, bebop dominant scale, Barry Harris 6th-diminished chords, enclosures targeting 3rds",
    "keys": "j/k drill · </> key in fourths · h hands (RH/LH/HT) · w wait/flow · c click (all beats / 2&4 jazz style / off) · [/] tempo · enter start/stop · esc leave",
    "modes": "wait = time stops until you play the right note (chords need every voice down); flow = the metronome rolls and every onset is judged: perfect/good/early/late within a ±90ms window",
    "ladder": "3 clean reps in a row → tempo +5 BPM automatically; your best clean tempo is saved per drill+key+hand in ~/.phosphor/practice.json and you resume there next session",
    "metrics": "bias (mean signed ms — rushing vs dragging), spread (consistency), evenness (IOI CV — under ~8% is conservatory-even), per-note verdict glyphs"
  },
  "loop_section": {
    "_about": "the loop brace is both a selection and a cursor: spc+l focuses it, and what's between the markers can be lifted and stamped anywhere",
    "grid": "g cycles the marker grid: bar / beat / 1/8 / 1/16 — the brace can shrink to a single 1/16 (below that a loop is a buzz); display reads bars ('1-4') or positions ('1.2-1.3' = bar.beat, '1.1.2' adds the 16th)",
    "move": "h/l move the start, H/L (or shift+h/l) the end, j/k slide the whole brace by one grid step, J/K leap it by its own length",
    "yank_cut": "y lifts the section (all instrument tracks, notes+controllers whose ONSET is inside; tails travel whole); x (or d) lifts and removes — one undo step across every track",
    "paste": "p stamps the lifted section at the brace start, REPLACING what's under the stamped span (and only the span — a track silent in the lift stamps its silence, tape-drop style), then leapfrogs the brace by the section length so p p p lays copies back to back; P layers instead of clearing (MPC-merge style). Clips union on landing, never overlap",
    "session": "loop markers now save in ticks; old bar-based sessions load exactly as before"
  },
  "sampler": {
    "_about": "the sampler puts a sound on every piano key: 88 pads, each stacking up to eight wav layers, with its own trigger/poly/choke/pitch/ADSR and per-layer tune/trim/reverse/mute",
    "create": "space then a opens the instrument menu — pick Sampler; the track opens on its [pads] tab with the keys on it",
    "pad_map": "the keyboard band across the top IS the kit: a key with sounds on it lights in the track's colour and carries the number of layers, a key whose file went missing is red, and the caret ▼ marks the pad being edited · the bed scrolls to keep the caret in sight on a narrow terminal",
    "pick_a_pad": "play a key on your MIDI controller — the pad under that key becomes the current pad (C3 to start) · h/l walk the bed one key at a time, H/L by an octave",
    "load": "a asks for a file: type a path and press enter — a bare name like 'kick' looks in ~/.phosphor/samples/ and tries .wav for you; each load stacks another layer on the current pad, up to eight, and points the panel at what just arrived",
    "knobs": "j/k picks a control, enter holds it, h/l turns it, H/L strides, esc lets go — held is the only time h/l stop walking the keyboard · the pad's own: trig · poly · choke · pitch · fine · attack · decay · sustain · release · level · pan · root · keytrk; then the selected layer's: level · pan · tune · fine · rev · mute",
    "layers": "[ and ] pick which sound on the pad the row knobs address, 1-8 jumps straight to one — one list, the wav/rec layers first and any phrases after them · m mutes the row (it keeps its seat) · d removes it and asks first · u brings it back with its audio or its notes, ctrl+r takes it off again",
    "audition": "moving the layer cursor with [ ] or 1-8 plays that sound on its own, outside the pad's poly and choke — a muted layer stays silent, and the audition stops the moment you leave the pads tab",
    "trim": "t on a layer that has audio opens the trim strip: the whole file as a waveform, the region that plays lit and the cut ends dark, [ and ] over the two markers · h/l move the START, H/L move the END — the loop brace's grammar · every start nudge plays the region from its new start",
    "trim_unit": "j/k walk how far one press moves: bar · beat · 1/16 · 10ms · 1ms · 1 sample (j deeper, k wider; it opens on 10ms) — the musical units come from the transport tempo measured against the layer's OWN sample rate, so a bar of a 48k loop is a bar wherever it is played",
    "trim_switches": "z toggles the zero-crossing snap (on by default: an edge lands on the nearest sign change within 5ms, and keeps the frame you asked for when there is none) · r plays the region backwards · t loops it while you work · esc goes back to the pad map and stops the sound",
    "trim_limits": "the edges never cross and stop a millisecond apart, which it says in words · a whole nudge run is one press of u · a layer whose file is missing is refused rather than trimmed",
    "hear_it": "press the same key — the pad plays its layers together; one-shot pads play whole, retriggers cut the last hit with a fade",
    "resample": "i on a pad asks which of our own instruments to record it from (everything except the sequencer and the sampler itself) — the track starts playing that instrument, the banner says so, and the pad cursor freezes because your keys are a performance now · p swaps what r will land, audio or phrase, and the banner reads it out · esc puts the sampler back with its kit",
    "resample_take": "r arms, r again ends the take and lands it on the pad as a rec layer, selected and named 'take 1', 'take 2' — stopping the transport ends it too, and esc ends a running take before it leaves the mode · the flash gives the length and the peak · one press of u takes it back off, ctrl+r brings it back with its audio",
    "resample_timing": "with the transport stopped the take is free: it starts on your first note, ends when you disarm, and the instrument's tail is rendered out to silence (2s at most, auto-trimmed −48 dB in / −60 dB out) · rolling, it is bar-quantised: the window opens at the next bar line and closes at the end of the bar you disarm in, so the take is whole bars and LOOPS — nothing past the loop point is kept, and nothing is trimmed",
    "resample_how": "what is recorded is MIDI with its arrival stamps; the audio is rendered afterwards, offline, through a fresh copy of the instrument and the track's own chord/arp devices — deterministic, so the same performance renders the same samples every time · caps: 60s free, 64 bars rolling, and a full pad refuses the arm before you play",
    "resample_root": "a performance on one key teaches the pad its root (keytrk is left alone — a root is a fact, keytracking is a decision); a chord leaves the root where it was · i again reopens the picker on the instrument the pad remembers, with the panel you recorded it with",
    "normalize": "n on a rec or wav layer sets its level so the trimmed region peaks at −0.5 dB — a gain value, never a rewrite of the audio · n again puts the level back to unity · our instruments render with a lot of headroom, so a quiet take can run out of level control (+12 dB) and the flash says how far short it landed",
    "phrases": "a phrase is the performance kept as notes instead of audio, replayed live through one instrument — four bars cost a few hundred events rather than a few megabytes · in source mode press p until the banner reads 'take: phrase', then r, play, r: the notes land on the pad as a phr row, chord/arp devices baked in exactly as they are for audio, one press of u deep · a one-finger phrase teaches the pad its root, and the pad remembers audio-or-phrase with the instrument it was recorded from",
    "phrases_child": "ONE child instrument per sampler, not one per pad — every phrase on every pad plays through it, so it is rendered once a block however many are running · landing a phrase points it at the instrument that phrase was played on, panel and all, and if that is a different instrument from the one it was pointed at the flash says 'child is now X · every phrase plays through it' · the child goes into the session and comes back with it",
    "phrases_controls": "a phrase's own controls are vel (the velocity scale every note is multiplied by, 0-400%, shown as a percentage because it scales what is played and not how loud the result is), mute, and keytrk (transpose by the played key's distance from the pad's root) · the pad's level, pan and ADSR do NOT reach a phrase and are not drawn as if they do, and neither do pan, tune or rev — there is one shared render for every phrase on the kit, so there is nowhere per-phrase to put them · choke, poly, one-shot/gate and the panic key all reach a phrase the way they reach a sample",
    "phrases_list": "phr rows sit after the wav/rec rows in the same list: name, length in seconds (frames at the engine's rate — the tempo you played at is baked in, a phrase does not follow a tempo change), the vel percentage, phr, and a mute dot · moving the cursor onto one does not audition it — a phrase has no waveform to sound on its own, so it says to press the pad's key instead · t on a phr row says a phrase is notes rather than opening an empty waveform · a phr row marked 'no instrument' is a sampler with no child, which only a hand-edited session can produce",
    "keys_mode": "K switches the bed between pads (every key its own sound) and keys (stretches of keys playing one sound, transposed from a root) — capital and deliberate, because lowercase k walks the knob cursor · both truths are kept: the pad map is still there under the zones and comes back untouched when you press K again, and the session file stores both",
    "keys_zones": "w covers the whole bed with one zone · o covers the octave the caret is in · s splits the zone under the caret at the caret key (the left half keeps its root, the right is rooted at its own first key) · inside a zone, w and o move that zone's edges instead of laying a second one over it · a new zone is seeded with the sound on the pad under the caret, or empty if there is none — then a loads one into it",
    "keys_brace": "the span is the first control on the panel: enter holds it, then h/l move the LOW edge and H/L the HIGH one — the loop brace's grammar, so H/L are the other end and not a stride · an edge stops where its neighbour begins and never pushes it, and stops at the ends of the bed · a whole run of presses is one press of u",
    "keys_zone_list": "the column beside the panel becomes the zone list: span, sound, layer count, root and how many keys · the band grows a rule under the keyboard drawing each zone's brace, with ┼ where its root is and ◆ on the root key itself",
    "keys_drop": "D takes the zone under the caret off the bed after a y/n confirmation — capital, because lowercase d still removes a layer from the zone's sound · u brings the zone back with its audio",
    "keys_root": "a root arrives three ways: a one-pitch take teaches it, a file name that ends in a note teaches it on the way in (Piano_C3, kick_A#1, Strings Eb2 — a name that is not a note changes nothing, and C3loop is not a note), or R arms the keyboard and the next key played becomes the root · esc disarms · the bar blinks ROOT? while it is armed, and the key played does not move the caret",
    "keys_edits": "everything else is the same keys pointed at the zone instead of the pad: j/k walks its controls, [ ] picks a sound inside it, t trims, n normalizes, i records into it · keytrk is not offered, because a zone always tracks the keyboard · where two zones overlap their layers stack, the ninth is turned away and the flash says so",
    "sessions": "saving stores each layer's path (never the audio); a file that moved is kept on its pad, marked red on the bed and 'missing' in the layer list — fix the path or drop the file in samples/ and reopen",
    "sessions_takes": "a recorded take has no file until you save: the save writes it as a 32-bit float WAV into <session>.samples/ beside the session file and stores the path relatively, so the project folder can be moved or copied whole · takes already on disk are not rewritten, saving under a new name gives the new project its own copies, and a take whose wav has gone missing keeps its pad and turns red like any other"
  }
}
